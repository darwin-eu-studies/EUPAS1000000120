# Example

This repository contains study code intended to run against an OMOP CDM database. Configure the connection settings described in [instructions.md](instructions.md), restore the R environment, and execute [CodeToRun.R](CodeToRun.R) to run the study workflow.
